export const YEARS_OPTIONS = Array(10).fill(1)?.map((i, index) => ({ label: index + 1, value: index + 1, key: index }));
export const MONTHS_OPTIONS = Array(12).fill(1)?.map((i, index) => ({ label: index + 1, value: index + 1, key: index }));
export const DAYS_OPTIONS = Array(31).fill(1)?.map((i, index) => ({ label: index + 1, value: index + 1, key: index }));